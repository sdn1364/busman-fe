import { ScrollArea } from "@/components/ui/scroll-area";

const Calendar = () => {
  return (
    <div className="h-full w-full">
      <ScrollArea>Calendar</ScrollArea>
    </div>
  );
};

export default Calendar;
